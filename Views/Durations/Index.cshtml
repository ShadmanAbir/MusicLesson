@model IEnumerable<MusicLesson.Models.Duration>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Length)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cost)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Length)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cost)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DurationID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DurationID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DurationID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
